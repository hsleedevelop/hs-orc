# hs-orchestrator — 구현 계획

- 버전: v0.1 (초안)
- 작성일: 2026-09-20
- 선행 문서: `docs/PRD.md`, `docs/SPEC.md`

## 원칙

- **가장 작은 end-to-end부터 완성하고, 검증된 상태 위에 확장한다.** 추상화를 먼저 세우지 않는다.
- 각 단계는 앞 단계가 **실제로 도는 것**을 확인한 뒤 시작한다.
- 게이트(`type-check → lint → test`)는 단계 종료 시 1회. 파일마다 반복하지 않는다.
- v1 Core는 UI를 모른다. v2에서 재작성이 필요하면 v1 설계가 틀린 것이다.

## 단계 개요

| 단계 | 산출물 | 완료 판정 | 선행 |
|---|---|---|---|
| S0 | 파이프라인·레이어·CI 게이트 | 빈 프로젝트에서 게이트 3종이 실제로 실패/통과한다 | — |
| S1 | 뼈대 e2e (primary 슬롯만) | 업무 3종이 서로 다른 엔진·모델·effort로 실제 프로세스를 띄운다 | S0 |
| S2 | 엔진 어댑터 3종 | 어댑터 테스트 통과 + 세 CLI 실제 실행 1회씩 | S1 |
| S3 | 라우팅 엔진 | 11행 테이블 테스트 통과, INV-1 위반 불가 | S2 |
| S4 | 진행 방식 3종 | 각 방식이 상한에 걸려 정상 중단된다 | S3 |
| S5 | TUI 셸 | 실제 작업 1건이 화면에서 끝까지 통과 | S4 |
| S6 | 관찰가능성 | 결정 로그 2회 append 확인 | S5 |
| **v1 릴리스** | | PRD §8 지표 전부 충족 | S6 |
| S7 | GUI 셸 (v2) | v1과 동일 시나리오가 GUI에서 통과 | v1 |
| S10 | 대화 세션 (v2.1) | PRD §7 v2.1 — 잡담·위임·후속이 project·scratch 양쪽에서 통과 | S9 |

---

## S0 — 파이프라인 먼저

코드보다 게이트를 먼저 세운다.

1. 프로젝트 초기화. 스택은 TypeScript(strict) 기준. 런타임·TUI 프레임워크는 S5에서 확정하되, Core는 런타임 중립으로 쓴다.
2. 레이어 경계를 디렉터리로 고정: `core/` `adapters/` `shell/` `data/`. `core/`가 `shell/`을 import하면 lint 에러.
3. CI(또는 pre-commit): `tsc --noEmit` + lint + test. `react-hooks/exhaustive-deps`는 해당 없음, 대신 `no-floating-promises`를 error로.
4. pre-commit 시크릿 스캔(gitleaks 등). `CURSOR_API_KEY` 같은 값이 커밋되지 않게 한다.
5. `matrix.json` 생성 스크립트: 원본 HTML의 `rows`/`profiles`를 파싱해 JSON으로 뽑는다. 첫 줄에 `// GENERATED — 소스: <경로>`.

**완료 판정**: 일부러 타입 에러를 넣으면 게이트가 실패하고, 고치면 통과한다. 설정만 있고 게이트가 없으면 완료가 아니다.

**위험**: 이 단계를 건너뛰고 S1로 가면 나중에 레이어가 섞인다. ebc 3저장소 실측에서 확인된 패턴이다.

---

## S1 — 뼈대 e2e

**여기서 멈추고 보고한다.** 이 단계가 안 돌면 설계 문제이지 다음 단계로 덮을 문제가 아니다.

범위: 작업 문자열 1개를 CLI 인자로 받아 → 11행 중 1행으로 분류 → **primary 슬롯만** 배정 → 해당 엔진을 비대화로 실행 → stdout 그대로 출력.

없는 것: UI, reviewer, 로그, 비용, 진행 방식, 증거 수집.

1. 분류기 최소 구현. 규칙 기반으로 시작한다(키워드 + 사용자 수동 선택). LLM 분류는 S3에서 필요하면 붙인다.
2. `matrix.json`에서 primary 모델·effort를 읽는다.
3. 모델 → 엔진 매핑 최소판(`SPEC §3.2` 기본 엔진 열만).
4. 프로세스 실행. 스트림 파싱 없이 stdout 패스스루.

**완료 판정**: 아래 3건이 서로 다른 엔진·모델·effort로 실제 프로세스를 띄운다.

| 입력 예 | 기대 배정 | 기대 명령 |
|---|---|---|
| "이 타입 에러 고쳐줘" | Luna·medium | `codex exec … -m <luna> -c model_reasoning_effort="medium"` |
| "이 기능 새로 구현해줘" | Sol·high | `codex exec … -c model_reasoning_effort="high"` |
| "이 아키텍처 설계 검토해줘" | Fable·high | `claude -p … --model <fable> --effort high` |

**위험**: 실제 모델 id(`<luna>` 등)를 CLI가 받는 형태로 확정해야 한다. `codex -m`이 받는 정확한 문자열을 S1에서 확인하고 `engines.json`에 박는다. 추측하지 말고 실행으로 확인한다.

---

## S2 — 엔진 어댑터

`EngineAdapter` 인터페이스(SPEC §3.1) 뒤로 세 엔진을 넣는다. **이 단계 이후 CLI 플래그 문자열은 어댑터 밖에서 보이지 않는다.**

1. `buildArgv()` — 세 엔진의 argv 생성. `codex`는 `exec` 서브커맨드(`-p` 아님).
2. **effort 검증** — 정규 어휘(`low|medium|high|xhigh|max`) 밖이면 **던진다**. `claude`가 잘못된 값을 경고만 내고 기본값으로 돈다는 것이 실측 확인됐으므로(SPEC §0.1-2) 여기가 유일한 방어선이다.
3. Cursor 어댑터 — effort가 모델 id 접미사다. `gpt-5.6-sol-xhigh` 형태로 조립하고 `-thinking`/`-fast` 변형 정책을 `engines.json`에서 읽는다. 바이너리는 `cursor-cli` → 없으면 `cursor-agent` 폴백.
4. `supports()` — 모델별 가용성. **Astra·Haiku는 Cursor에 없다**(실측). 지원하지 않는 조합 요청은 명시적 실패이며, 가장 가까운 모델로 말없이 바꾸지 않는다.
5. `--output-format stream-json` 파싱. **중첩 try**로 감싸 한 줄 파싱 실패가 실행 전체를 죽이지 않게 한다. 원본 stdout/stderr는 실행별로 보존한다.
6. 취소·타임아웃. 프로세스 그룹 단위 종료.

**완료 판정**
- 어댑터 단위 테스트: 세 엔진 기대 argv, 잘못된 effort가 던짐, Astra+cursor가 실패함
- 통합: 세 CLI로 짧은 실제 프롬프트 1회씩 성공
- 취소 테스트: 실행 중 취소 후 자식 프로세스가 남지 않음 (좀비 없음)

**위험**: 좀비 프로세스는 비용이 계속 나간다. 취소 테스트를 빼지 않는다.

---

## S3 — 라우팅 엔진

파이프라인 전체(SPEC §4)를 완성한다.

1. **Gatekeeper** — `/delegation-router` §1 하한선 6개 점검. 걸리면 배정하지 않고 종료한다. **순서가 핵심이다**: 하한선이 배정보다 앞이다.
2. **Assigner** — 두 슬롯 배정. `INV-1` 교차 벤더 검사를 타입 수준에서 강제할 수 있으면 그렇게 하고, 아니면 생성 시 검사 후 실패.
3. **비용 산정** — primary + reviewer 합산. AA 측정치임을 값과 함께 운반한다.
4. **상향 사다리** — L1→L5. 상향 요청 시 `근거 보강 → effort → 모델 → reviewer` 순서를 강제하고, L5 직행 경로를 만들지 않는다.
5. **"해당 없음"** — 사용자에게 올린다. 기본 배정을 만들지 않는다.
6. 분류기 개선 — 규칙으로 부족하면 저비용 모델(Haiku·low 또는 Luna·low)로 분류만 시킨다. 분류에 Fable을 쓰지 않는다.

**완료 판정**
- 11행 전부 테이블 테스트 통과, `matrix.json`이 원본 HTML과 대조 테스트 통과
- INV-1 위반 조합을 만들려는 시도가 실패
- 하한선에 걸리는 입력이 엔진을 띄우지 않음 (프로세스 spawn 0회 확인)
- 상향 요청이 사다리를 건너뛰지 않음

**위험**: 매트릭스를 단일 엔진 선택기로 축소하는 것. reviewer 슬롯이 살아 있는지 매 커밋에서 확인한다.

---

## S4 — 진행 방식 3종

**공통 인프라 먼저**: 사이클 단위 기록(근거·변경·검증), 최대 시도 카운터, 누적 비용 추적, 상한 도달 시 에스컬레이션. 세 방식이 이것을 공유한다.

### S4-a `/pingpong` — 대화형
턴 단위 진행. 턴 종료 시 결과·다음 제안을 제시하고 **사용자 입력을 기다린다.** 사용자는 방향 수정·배정 변경·중단이 가능하다. 최대 턴 제한 없음(매 턴 사용자 승인). 누적 비용 상시 표시.

### S4-b `/loop` — loop-engineering
Goal / Planner / Executor / Evaluator / Critic / Recovery / Stop을 **분리된 구성요소**로 구현한다. Evaluator에는 reviewer 슬롯 모델을 쓴다 — 독립 리뷰가 루프 안에서 실현되는 자리다.
**최대 반복 수 없이는 구현하지 않는다.** 누적 비용 상한 도달 시 강제 중단.
> 미결: 내장 `/loop` 스킬 활용 여부. 기본안은 자체 구현(SPEC §6.2).

### S4-c `/graph` — graph-engineering
노드별 독립 분류·배정. **순환 검출은 실행 전 필수**. 병렬 실행은 **쓰기 대상 파일 비겹침이 확인된 경우만**, 불확실하면 순차로 떨어뜨린다. 부분 실패 전파 규칙(`fail-fast` / `skip-dependents` / `continue`)을 노드마다 선언. 최대 노드 수와 누적 비용 상한 강제.

**완료 판정**: 세 방식 각각이 상한(반복 수 / 노드 수 / 비용)에 걸려 **정상 중단**되는 것을 테스트로 보인다. 정상 완료보다 이 경로를 먼저 검증한다.

**위험**: 무한 루프는 비용이 직접 나간다. 상한 없는 방식은 머지하지 않는다.

---

## S5 — TUI 셸

S1~S4가 전부 헤드리스로 돈 뒤에 붙인다.

1. TUI 프레임워크 선택. 이 시점의 Core는 UI에 의존하지 않으므로 교체 비용이 낮다.
2. 화면 5개 (SPEC §7): Run / Tasks / Dashboard / Sessions / Reviews.
3. **비용은 실행 전에 보인다.** "AA 벤치마크 측정치, 실제 지출 아님" 표기 포함.
4. 근거 등급 배지 분리 표시. TB4 0.3pp 차이로 우열 표시하지 않는다.
5. Sessions: `codex agents` + `claude agents` 통합.
6. Reviews: `gh-axi` 우선, `gh` 폴백.
7. 디버그 화면을 프로덕션 빌드에 포함. 화면 타이틀에 환경 + 버전.

**완료 판정**: 실제 작업 1건이 입력 → 분류 → 배정·비용 확인 → 승인 → 실행 → 증거 수집까지 화면에서 통과한다.

---

## S6 — 관찰가능성

1. 결정 로그 append (SPEC §8). 배정 시 1차, 검증 후 **같은 `id`로 2차**.
2. 실행별 원시 로그 보존.
3. 에러 리포팅에 태그와 모듈 출처.
4. 정상 비즈니스 상태(결과 없음·분류 실패)를 에러로 올리지 않는다. `catch` 후 무동작 금지 — 실패에는 사용자에게 보이는 상태가 있어야 한다.

**완료 판정**: 작업 1건 실행 후 jsonl에 같은 `id`의 줄이 정확히 2개 있다.

---

## v1 릴리스 게이트

PRD §8 지표를 실제 실행으로 확인한다. **2026-09-20 실측 (커밋 f3ef07c 기준):**

- [x] 매트릭스 11행 배정 일치율 **11/11 = 100%**
- [x] 교차 벤더 위반 **0건** (INV-1 이 타입 수준으로 막는다)
- [x] 실행 전 비용 미표시 경로 **0건** — 게이트를 돌리며 **2건을 찾아 고쳤다**:
      `--mode loop` 는 1사이클 비용만 찍고 최대 반복을 안 보여줬고,
      `--mode graph` 는 분류 결과의 비용을 찍을 뿐 **노드별 배정 합계**가 아니었다
- [x] 증거 없이 완료 표시된 작업 **0건** — 자동 증거가 없으므로 성공해도 `outcome: unverified` 다.
      `outcome: ok` 로 닫힌 줄이 0건이라는 뜻이고, 이것이 정직한 상태다
- [x] 결정 로그 2차 append 누락 **0%** (4건 / 누락 0)
- [x] `matrix:check → type-check → lint → test` 전부 **exit 0**, 테스트 **120 pass / 0 fail**
- [x] 세 엔진 실제 실행: codex(luna·low) 15.5s · claude(haiku·low) 11.3s · cursor(luna·low) 23.8s,
      셋 다 `ok`, 파싱 실패 0줄

**2026-09-20 2차 — 위 두 구멍을 메웠다:**

- [x] **증거 수집기**(SPEC §5) — 11행 요구 증거 표를 코드로 옮겼다. `--verify "[phase:]<명령>"` 으로
      명령을 실제로 돌려 **exit code** 를 받고, 변경 파일은 git 에서 읽는다. 사람이 적어야 하는
      문서 절·리뷰·측정은 `--evidence <file.json>` 이다.
      **모양이 아니면 거절한다**: exit code 없는 명령, `file:line` 아닌 인용, 환경 없는 측정값, 빈 절.
      실측: 증거 없이 실행 → `unverified`, `--verify` 통과 → **`outcome: ok`**.
- [x] **크래시 리포터 자가 검증** — CLI `--crash-test`(exit 3)와 TUI Debug 화면 `c` 키.
      둘 다 `[error][<module>/crash-test]` 로 리포팅 경로가 살아 있음을 보인다.

**여전히 남는 것:**
- 증거 수집은 **선언으로 받는다.** 업무 유형만 보고 검증 명령을 추론하지 않는다 —
  프로젝트마다 다르고, 틀린 추론은 "증거로 닫았다"는 거짓말이 된다.
  대신 `data/verify.json` 에 행별 기본 명령을 **선언**하면 매 실행 `--verify` 를 다시 적지 않아도 된다.
  **선언이 없으면 빈 배열이고**, 증거가 없으면 `outcome` 은 `unverified` 로 남는다.
- ~~Q4·Q6~~ → **D-022·D-023 확정 (2026-09-21).** 미결 질문은 이제 없다(Q1~Q7 전부 종결).
  - D-022: 미분류가 임계치(3회) 이상 쌓이면 **제안만** 한다. 행 추가는 원본 HTML 수정 → `gen:matrix` 로만.
  - D-023: Cursor `-fast`는 **모델별 옵트인**이고 없는 모델(Sonnet·Fable)엔 던진다.

---

## S7 — GUI 셸 (v2)  ✅ 2026-09-21

`shell/` 만 교체한다. Core·adapters는 손대지 않는다.

**완료 판정**: v1의 S5 완료 시나리오가 GUI에서 동일하게 통과한다.
**위험**: Core 수정이 필요하면 v1 설계가 틀린 것이다. 그 경우 GUI를 진행하지 말고 Core 경계를 먼저 고친다.

**실측 결과 (Electron 44.4.3 + React 19, D-021):**

- **위험 항목 통과** — `git diff main -- src/core src/adapters src/data` 가 **비었다.**
  Core·adapters·data 를 한 줄도 고치지 않고 GUI 가 붙었다. v1 설계가 틀리지 않았다는 유일한 증거다.
- **실제 창에서 S5 시나리오 통과** (`webContents.executeJavaScript` 로 UI 를 직접 조작):
  1. 입력 → `분류 R01` · `primary Luna·medium → codex/gpt-5.6-luna` · `reviewer Haiku·low → claude/claude-haiku-4-5-20251001` · `비용 $0.39` · `INDEPENDENT` 배지
  2. **승인 버튼 클릭** → 실제 codex 프로세스 실행
  3. `증거 충족` · `outcome=ok` · `누적 $0.18 / $20 (추정 포함)`
  4. Dashboard 반영: `배정 분포 Luna×1` · `결과 ok×1` · `검증 누락 없음`
- 결정 로그: 같은 `id` 2줄 (`pending` → `ok`)
- 화면 5개(Run/Dashboard/Sessions/Reviews/Debug) + **고의 크래시 버튼**을 프로덕션에도 싣는다
- 렌더러에 Node 를 열지 않는다(`contextIsolation: true`, `nodeIntegration: false`).
  Core 접근은 preload 가 노출한 IPC 뿐이고, CSP 로 원격 코드·인라인 스크립트를 막는다

**비용**: Electron 바이너리로 `node_modules` 가 72MB → **403MB** 가 됐다. 이 저장소는 iCloud Drive 안이라
그만큼 동기화된다 — 신경 쓰이면 `node_modules` 를 iCloud 밖으로 빼거나 `.nosync` 처리를 검토한다.

---

## S8 — 진입점  ✅ 2026-09-21

`bin/hs-orc.mjs` 하나. `shell/` 에만 붙는다(D-001) — Core·adapters·data 무변경. 빌드 산출물 없음(D-019, D-024).

**완료 판정 — 실측 (저장소 바깥 `/tmp` 의 빈 git repo, `~/…/bin` 대신 임시 PATH 디렉터리의 심볼릭 링크로 호출):**

- [x] `hs-orc "broken.ts 의 타입 에러를 고쳐라"` → `R01` 분류 · `primary Luna·medium(codex)` / `reviewer Haiku·low(claude)` · `$0.39` 제시
- [x] `--run` 이 **두 슬롯을 실제로** 띄웠다 — Luna 실행 65.6s → Haiku 독립 검증 `FAIL`(누락 3건 지적)
- [x] 그 디렉터리에 `.hs-orc/runs/0921-0107-df6/{01-Luna,02-Haiku}.{stdout,stderr,meta.json}` 생성
- [x] 결정 로그 2줄(`pending` → `unverified`) — 증거(기존 test exit code)가 없으니 `unverified` 가 정답이다
- [x] 심볼릭 링크로 불러도 설치 위치를 찾는다 (Node 가 main 을 realpath 로 해석한다)
- [x] 게이트 `matrix:check → type-check → lint → test` **exit 0, 173 pass / 0 fail**

**실사용에서 드러난 것 (진입점 결함이 아니다):**
codex 슬롯이 read-only 샌드박스로 떠서 파일을 고치지 못하고 "수정안"만 냈다. reviewer 가 그것을 `FAIL` 로 잡았다 —
두 슬롯 설계가 의도대로 작동한 증거이지만, **primary 에 쓰기 권한을 주는 방법이 SPEC 에 없다.** 다음 작업이다.

**남는 것:** `~/.local/bin` 실설치(브랜치 머지 후), 실사용 1건, 비용 실비화, README.

---

## S9 — 실사용 1건  ✅ 2026-09-21 (PRD §7 v1 완료 판정 충족)

PRD §7 은 v1 을 **"실제 작업 1건이 분류→배정→실행→증거 수집까지 사람 개입 없이 통과"** 로 정의한다.
그동안 실행은 전부 인위적 프롬프트였고 `outcome` 은 항상 `unverified` 였다. 이번이 첫 통과다.

**작업**: "src/shell/tui 와 src/shell/gui 에서도 primary 슬롯 쓰기 권한(D-025)을 켤 수 있게 노출하라."
**명령**: `hs-orc "<작업>" --classify-llm --write --timeout 600 --run` (한 줄, 중간 개입 없음)

| 단계 | 결과 |
|---|---|
| 분류 | 규칙 무매치 → Haiku·low 폴백이 **R01** 선택 |
| 배정 | primary Luna·medium(codex) / reviewer Haiku·low(claude) · $0.39 |
| 실행 | Luna 가 셸 10개 파일 수정 (251s) |
| 검증 | Haiku 독립 리뷰 통과 |
| 증거 | `npm test` **exit 0** + changed-files + review → **증거 충족** |
| 종료 | **`outcome: ok`** · 실비 $0.3784 · 결정 로그 2줄 |

산출물은 사람이 따로 검증했다: `npm run gate` **exit 0, 180 pass**, TUI `--write` 실렌더 확인, GUI 번들 통과.

**이번에 드러난 것**

1. **`data/verify.json` 이 비어 있는 한 무엇을 해도 `unverified` 다.** 그동안의 `unverified` 는 제품 결함이 아니라 **선언 부재**였다. 이 저장소 몫으로 R01·R04·R06 을 선언하니 곧바로 `ok` 가 나왔다. 다른 프로젝트에 설치하면 그 프로젝트가 자기 명령을 적어야 한다 — 설치 안내에 반드시 들어가야 한다.
2. **규칙 분류기가 평범한 작업 문장을 놓친다** — ✅ 2026-09-21 D-026 으로 종결. "…켤 수 있게 하라" 는 R03 키워드(`구현해`·`만들어`)에 안 걸린다. 표는 명령형 어미 몇 개에 의존한다. `--classify-llm` 이 구제했다. → **D-026**: 폴백을 기본으로 켜고 돌 때마다 비용을 찍는다. 곁다리로 모르는 `--옵션` 이 조용히 작업 문자열이 되던 구멍도 막았다. 폴백은 `core/pipeline.ts` 의 `routeWithFallback` 한 곳에 있고 **CLI·TUI·GUI 가 전부 이것을 쓴다** — 셸마다 다르게 동작하지 않는다(실측으로 셋 다 같은 문장에서 R03 으로 붙는 것을 확인).
3. **테스트가 저장소와 사용자의 개인 로그를 오염시킨다** — ✅ 2026-09-21 수정. `shell/__tests__/cli.test.ts` 만 샜다 — 유닛 테스트는 파일 경로를 명시로 받지만 이 파일은 **진짜 CLI 를 자식으로 띄워** 기본 경로가 그대로 적용됐다. 피해 두 가지:
   - 저장소 `.hs-orc/unclassified.jsonl` 에 `'오늘 점심 뭐 먹지'` 가 쌓여 D-022 임계치(3회)를 넘겼고, 제품이 **실제로 "행을 추가하라"는 가짜 제안**을 냈다.
   - 사용자의 개인 결정 로그 `~/.claude/logs/delegation-router.jsonl` 에 **일어나지도 않은 $10.89 실행이 38줄** 남았다. 이쪽이 더 나쁘다 — 저장소 밖이고 gitignore 도 못 막는다.

   **수정**: cwd 기준 경로는 임시 cwd 로, 홈 기준 경로는 `HS_ORC_DECISION_LOG` 로 돌렸다. 그리고 **격리가 풀렸는지 재는 테스트**를 같은 파일에 넣었다 — 자식을 띄우기 전에 저장소 로그 크기를 찍고 끝에 대조한다. 음성 대조: 격리를 풀면 그 가드만 깨진다.
4. **샌드박스 안의 primary 가 돌린 검증과 증거 수집기의 결과가 다를 수 있다.** Luna 는 자기 실행 중 `npm test` 를 돌려 1건 실패(`~/.claude/logs/delegation-router.jsonl` 에 쓰려다 `EPERM`)를 봤다고 보고했다. 결정 로그 기본 경로가 **워크스페이스 밖**이라 `-s workspace-write` 에 막힌 것이다. 증거 수집기는 샌드박스 밖에서 돌아 exit 0 이었다 — **증거는 수집기 쪽이 옳다.** 다만 primary 의 자기 보고와 증거가 갈리는 구조라는 것을 알고 있어야 한다.

---

## S10 — 대화 세션 (v2.1)  ⚠️ 2026-09-23 부분 통과

상세 계획: `docs/superpowers/plans/2026-09-23-conversation-session.md` (12 작업, subagent-driven). 브랜치 `feat/v2.1-conversation-session`.
구현 11 작업 + 최종 리뷰(Important 3건 수정) + 보류 결함 1건 수정. 게이트 299/299.

**실사용 판정** (실제 Electron + 실제 엔진, CDP 로 `window.orc.conv*` 구동 — 화면 자체는 목 브리지로 따로 확인):

| 항목 | project (이 저장소, 쓰기 끔) | scratch (`~/.hs-orc/scratch/…`) |
|---|---|---|
| ① 잡담 → 직접 답 (G6) | 통과 — 폴백 안내 줄 + 비용 $0.0413 | 통과 — $0.0411 |
| ② 위임 → 승인 → 두 슬롯 → 결과·요약 | 통과 — R01 Luna·medium, reviewer FAIL, `unverified`, 사다리 제안 | 결과까지 통과 (codex 가 git 밖에서 돈다 — nonGit). **요약은 토큰 상한으로 멈춤**(오류 줄로 보임) |
| ③ 앞 결과를 참조한 후속 (G7) | **어긋남** — "방금 위임 결과를 한 줄로 다시 설명해" 가 hint 받은 LLM 분류로 R02 배정 | 토큰 상한으로 직접 답 시작 안 함 (오류 줄) |
| ④ 같은 슬롯 resume | 통과 — 두 primary 가 같은 codex thread `01a0ce53…` (원시 로그 2건), 엔진이 앞 실행을 기억해 답, 97초 → 42초 | (시나리오상 생략) |
| ⑤ 닫고 다시 열기 | 통과 — 14건 동일 | 통과 — 10건 동일 |

**판정: 부분 통과.** 흐름·기록·resume·재열기·상한 표시는 설계대로 돈다. 막힌 것 두 가지는 코드 결함이 아니라 **정책 값과 분류 경계**다:

1. **토큰 상한이 대화 세션 규모에 맞지 않는다.** 앱 수명 동안 하나인 `Budget`(D-030 `tokenBudget` 200만)이 codex 위임 약 2.5회에 닿았다 (위임 1회 수십만 토큰 — 대부분 캐시 입력). 원인 후보는 Q13: 위임 엔진이 사용자 전역 hook·skills·CLAUDE.md 를 싣고 뜬다 — 지휘자(Haiku) 답에 "전하" 호칭이 나오고, 직접 답 1회가 $0.001 예상 대비 $0.04 다.
2. **후속 "설명 요청" 이 위임으로 분류된다.** hint(직전 요약)를 받은 LLM 폴백이 대화성 후속을 업무 행(R02)으로 붙인다. 사용자는 거절 후 다시 보내야 한다.

**후속 (이 계획에서 고치지 않은 것):**
- 위 1·2 — 결정이 필요하다 (상한 단위: 앱 수명 vs 세션 / Q13 격리 / 분류 폴백이 "대화 후속"을 NONE 으로 둘 기준).
- ~~분류 폴백(Haiku) 호출은 `Budget` 에 과금·상한 판정되지 않는다~~ → **D-034 해결** (받은 예산에 과금, 상한이면 시작하지 않는다). 리뷰 Minor 중 "분류 엔진 실패가 '맞는 행 없음'으로 표시"도 해결 (`failed` + 사유).
- 분류기 후보 luna(codex)는 격리 인자 선언이 없어 실행 전에 던진다 (D-032 B1). 그래서 분류 폴백의 **metered 경로는 지금 도달할 수 없고** 끝-끝 테스트가 없다 (D-034 리뷰 Minor) — Haiku 는 claude 가 금액을 주므로 단가를 일부러 비웠다. codex 격리를 실측·선언하면 그때 테스트를 붙인다. `meteredUsd` 계산 자체는 `pricing.test.ts` 가 덮는다.
- `delegate()` 의 journal `charge` 는 reviewer 가 돌면 reviewer 의 charge 다 (`GuiService.run` 때부터).
- git 이 아닌 **project** 폴더에서 codex 위임은 여전히 거절된다 (nonGit 은 스크래치에만).
- 다른 cwd resume, resume 중 모델 변경, 긴 세션 압축 — 미실측. codex 는 resume 에 쓰기를 실을 수 없어 쓰기 켠 후속은 잇지 않는다 (최종 리뷰 #1).
- GuiService 는 활성 세션 하나. CLI·TUI 는 아직 `ConversationSession` 을 쓰지 않는다 (D-031 결정 8).
- 최종 리뷰의 Minor 목록(세션 목록 상태 열 없음, 거절이 결정 로그에 안 남음, 렌더러의 IPC 실패 후 낡은 뷰 등)은 `.superpowers/sdd/…/final-review.md` 에만 있다 — 병합 전 이 절로 옮길지 정한다.
- Q12 스크래치 보존·정리.

---

## 단계별 위험 요약

| 단계 | 최대 위험 | 완화 |
|---|---|---|
| S0 | 게이트 없이 진행 | 일부러 실패시켜 게이트 동작 확인 |
| S1 | 모델 id 추측 | 실행으로 확인 후 `engines.json`에 박음 |
| S2 | 조용한 effort 폴백, 좀비 프로세스 | 어댑터 사전 검증 + 취소 테스트 |
| S3 | 매트릭스를 단일 선택기로 축소 | reviewer 슬롯 존재를 테스트로 고정 |
| S4 | **무한 루프 = 직접 비용** | 상한 없는 방식 머지 금지 |
| S5 | Core에 UI 누수 | lint 규칙으로 import 방향 강제 |
| S6 | 로그 2차 누락 | 작업 종료 경로 전부에서 append 확인 |

## 사람에게 올리는 조건

- 실제 비용이 발생하는 실행을 처음 띄우기 전 (특히 Astra·Fable max 조합, 합산 $10.89)
- 되돌리기 어려운 변경, 외부 쓰기
- 매트릭스 "해당 없음" 분류
- 상한(반복·노드·비용) 도달
- 매트릭스 원본과 구현 테이블 불일치 — **구현을 멈추고 보고한다**
- CLI 실제 플래그가 SPEC §0과 다를 때 — 추측한 플래그로 진행하지 않는다

## 중단 조건

- S1이 안 도는데 S2 이후로 넘어가지 않는다
- 같은 실패를 2회 연속 같은 방식으로 재시도하지 않는다
- 상한 없는 반복 방식은 머지하지 않는다
