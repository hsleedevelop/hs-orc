# Handoff: loop-gates-d040-d049
- from: local_facfb620-2347-445c-b85a-fd45a8f45d7e ("Measure CLI loop retry with real engines") · cwd: /Users/hsonpro/Library/Mobile Documents/com~apple~CloudDocs/shared_workspace/hs-orc/.claude/worktrees/strange-cori-9fb734 · branch: claude/test-conflict-escalate (머지됨, origin/main = a25552f) · 2026-09-25T18:05+09:00 · model: claude-opus-5-5 · 형식: full · 전달: B task_61c390a0 · 종결: 2026-09-25 funny-cannon-0fa64d

## 1. 목표
원래 요청: D-036 loop 재시도의 규모를 실제 엔진으로 최소 측정하고(R01, 승인 후 실행), 측정값을 DECISIONS 에 기록. 이후 사용자 요청으로 측정 중 드러난 결함·정책을 차례로 고쳤다.
범위 제외: 실제 엔진 실행은 매번 명시적 승인(명령·행·예상 토큰·최대 사이클·토큰 상한) 없이는 하지 않는다 — 사용자의 구독 사용량이다.

## 2. 현재 상태
- 완료: PR #9~#27 전부 main 에 squash 머지(최신 a25552f). 결정 D-040~D-049 기록.
  - 실측(모두 docs/DECISIONS.md 에 수치): D-036 규모(1사이클 45만), D-041(--write 2사이클 67만), D-042 실제 CLI(엔진 0회, trap PATH), D-036 critique 통제 실측(83만, 1사이클 reviewer 합성), D-047 실제 엔진(65만).
  - 코드: loop stdout(#10), deps 오탐(#11), D-040 Core 검증 실행, D-041 reviewer 생략, D-042 기준선·--fix-red-baseline, D-043 rework, D-044 once exit 1, D-045 phase 시점 매핑, D-046 notRun(126/127/-1), D-047 기존 테스트 줄 추가만 허용(verify.json tests), D-048 의존성 없음 차단, D-049 과제·테스트 충돌 조기 escalate, #25 codex 메시지 빈 줄 구분, #26 reviewer "다시 실행하지 마라".
- 진행 중: 없음.
- 미착수(후보): B — #26·#27 효과 실측(엔진 승인 필요, 약 45만~65만 추정). A — 머지된 원격 origin/claude/* 브랜치 19개 정리(삭제 승인 필요).

## 3. 결정과 기각한 대안
- 결정 근거·기각안은 전부 docs/DECISIONS.md D-040~D-049 각 절의 **기각** 에 있다. 요지:
- 판정은 exit code·git·파일 내용 등 **기계적 신호만** — 출력 패턴 파싱·과제 문구 의도 추론은 매번 기각(SPEC §5 "추론하지 않는다").
- 사람이 풀 문제(기준선 빨강, 재현 안 됨, 환경 오류, 의존성 없음, 과제·테스트 충돌)는 엔진 전에/재시도 없이 escalate. 모델이 풀 문제(테스트 실패·약화 1회)는 재시도.
- D-047·D-049 는 사용자 위임에 따라 권장안으로 가정해 정했다(상태 줄에 명시).

## 4. 건드린 파일
- 이번 세션 전체 변경은 main 에 머지됨: src/shell/cli.ts(loop·once 분기 대부분), src/core/evidence.ts(contradiction·notRun·outcomeOf·test-files), src/core/evidence-gather.ts(snapshotTests·testChanges), src/core/modes/loop.ts(lastOutput·reviewerSkipped), src/core/duo.ts(reviewPrompt checks), src/core/delegate.ts, src/data/verify.ts(declaredTests), src/adapters/run.ts, src/shell/deps.ts, data/verify.json(tests), README.md(exit code 표), docs/SPEC.md §5·§8, docs/DECISIONS.md.
- 미커밋 diff: 없음(git status --short 비어 있음). 이 인계 파일만 워크트리의 .claude/handoffs/ 에 새로 생긴다(메인 체크아웃 .claude/ 쓰기는 워크트리 훅이 막았다).

## 5. 검증 상태
- 실행: `npm test` → PASS 367건 (a25552f 기준, 머지 전 CI gate 2건 PASS).
- 실행: 각 PR 의 새 테스트는 수정 전 코드에서 FAIL 확인 후 머지.
- 미검증: #26(reviewer 재실행 금지)·#27(D-049 조기 escalate)의 실제 엔진 효과 — 추정뿐. D-048 은 가짜 바이너리로만 확인. 모든 실측은 n=1·대조군 없음(스크래치 clamp 저장소).

## 6. 다음 한 걸음
사용자에게 B(#26·#27 효과 실측) 계획을 제시하고 승인받는다 — D-047 실측과 같은 스크래치 fixture·shim 방식, `--max-iterations 2`, 예상 토큰과 성공 기준 포함. 엔진은 승인 전 실행 금지.
- 그 다음: A(원격 claude/* 브랜치 19개 정리 — 목록 제시 후 삭제 승인).
- 선택: D-048 의 선언형 preflight(E)는 실측 사례가 생기면.

## 7. 사용자에게 열린 질문 / 블로커
참고: B·A 모두 사용자 승인 사항(엔진 사용량·삭제). §6 은 "계획 제시" 까지라 질문 없이 진행 가능.

## 8. 환경 함정
- 저장소 경로에 공백(iCloud) — 항상 따옴표.
- 새 워크트리는 deps 없음: `node_modules -> "/Users/hsonpro/Library/Mobile Documents/com~apple~CloudDocs/shared_workspace/hs-orc/node_modules.nosync"` 심볼릭 링크.
- node 설치 디렉터리(`~/.local/share/mise/installs/node/latest/bin`)에 **진짜 codex** 가 같이 있다 — 엔진 차단 격리 시 `trap/`(엔진 이름 가짜) + `nodeonly/`(node 심링크, npm·npx 는 **래퍼 스크립트** — 심링크면 MODULE_NOT_FOUND) + `/usr/bin:/bin` 순서, 실행 전 `command -v` 로 확인.
- 스크래치 fixture·shim·로그는 이전 세션 scratchpad(/private/tmp/claude-501/.../scratchpad/: loop-measure, shim3/shim4, run1~run4, d042) — 세션 전용이라 새 세션에서는 다시 만들어야 할 수 있다.
- auto-mode classifier 가 `--run` 없는 dry run 도 한 번 막았다(승인 뒤엔 통과).
- 워크트리 세션은 메인 체크아웃의 .claude/ 에 쓸 수 없다(PreToolUse 훅) — 이 인계 파일이 워크트리 안에 있는 이유. 이 워크트리를 지우기 전에 메인으로 옮긴다.
- 커밋 메시지: 한국어 conventional + `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`. PR 본문 끝 `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. 머지는 squash.

## 9. 새 세션이 먼저 읽을 것
- docs/DECISIONS.md (D-036 규모·critique 실측, D-040~D-049)
- src/shell/cli.ts (loop 분기)
- src/core/evidence.ts
- data/verify.json
- ~/.claude/CLAUDE.md 의 실제 엔진 승인 규칙(대화 첫 지시와 같음)

## 인계 종결
사유: 수행함 — §6(B 실측 계획 제시)까지 실행, 남은 것은 사용자 승인뿐.
- 한 것: funny-cannon-0fa64d 워크트리를 origin/main a25552f 로 ff, node_modules 심링크, `npm test` PASS 367 재확인. B 계획(R-a #26 reviewer 재실행 금지, R-b #27 D-049 조기 escalate) 제시.
- 남은 것: B 승인·실행(엔진 사용), A 원격 claude/* 브랜치 정리(삭제 승인).
- 함정: 이 파일은 여전히 strange-cori-9fb734 워크트리에만 있다 — 그 워크트리를 지우기 전에 메인 .claude/handoffs/ 로 옮긴다.
